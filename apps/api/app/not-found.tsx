export default function NotFound() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>404 — Page introuvable</h1>
      <p>
        Cette API expose des routes sous <code>/api/…</code> (ex.{" "}
        <code>/api/health</code>).
      </p>
    </main>
  );
}
